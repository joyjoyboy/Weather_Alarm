<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="wenxiao.com.weatherapp.Login">


    <TextView
        android:text="Login:"
        android:id="@+id/login"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="25dp"
        android:paddingBottom="10dp" />

    <EditText
        android:id="@+id/userName"
        android:hint="User Name"
        android:layout_height="wrap_content"
        android:layout_width="fill_parent"
        android:layout_below="@id/login"
        android:paddingBottom="10dp" />

    <EditText
        android:id="@+id/password"
        android:hint="Password"
        android:layout_height="wrap_content"
        android:layout_width="fill_parent"
        android:password="true"
        android:layout_below="@id/userName" />

    <Space
        android:layout_height="30dp"
        android:layout_width="fill_parent"
        android:id="@+id/paddingSpace"
        android:layout_below="@id/password"/>

    <Button
        android:id="@+id/signIn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Sign in"
        android:textSize="20dp"
        android:layout_below="@id/paddingSpace"
        android:paddingTop="10dp" />

    <TextView
        android:text="Does not have a user name?"
        android:id="@+id/signUpReminder"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="15dp"
        android:paddingTop="15dp"
        android:layout_below="@id/signIn"/>

    <Button
        android:id="@+id/signUp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Sign up"
        android:textSize="20dp"
        android:layout_below="@id/signUpReminder"
        android:paddingTop="10dp" />

</RelativeLayout>
